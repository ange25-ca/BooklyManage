doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Document
  body 
    block content
      .general-container
        form(action='', method='GET' class='search-bar')
          input(type='text', name='q', placeholder='Buscar producto...')
          button(type='submit')
            i(class='material-symbols-rounded') search